import React from "react";
import ApplyCard from "../components/modules/Apply/ApplyCard";

const Apply = () => {
  return (
    <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', minHeight: '80vh' }}>
      <ApplyCard />
    </div>
  );
};

export default Apply;
